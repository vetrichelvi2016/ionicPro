var snap = snap || {};

(function ($, $snap) {
    var snap = {
        utilities : {
            browsers: {
                chrome: "56",
                firefox: "51"
            }
        }
    };

    $.extend($snap, snap);

})(jQuery,snap);