(function($, $snap) {
    var snap = {
        utilities : {
            browsers: {
                chrome: "XX",
                firefox: "YY"
            }
        }
    };

    $.extend($snap, snap);

})(jQuery,snap);